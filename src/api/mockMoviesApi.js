/**
 * Ранее здесь был отдельный клиент Gemini с захардкоженным ключом (небезопасно).
 * Сохраняем точку входа для обратной совместимости импортов — реализация в aiService (Groq + env).
 */
export { askMovieAI } from './aiService.js';
